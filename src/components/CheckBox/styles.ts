import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: 24,
    height: 24,
    borderRadius: 22,
    borderWidth: 2,
    alignItems: "center",
    justifyContent: "center",
  },
  marker: {
    height: 10,
    width: 14,
  },
});
